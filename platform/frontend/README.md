# ResearchFlow Frontend

This folder contains an optional Next.js interface used during development.

The normal open-source workflow starts from the root README and local
`.claude/skills`; this frontend is optional.

For frontend development from the repository root:

```bash
cd platform/frontend
npm install
npm run dev
```

Open <http://localhost:3000> in a browser.
