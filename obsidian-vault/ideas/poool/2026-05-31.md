# 直接启发
## [[analysis/3DV_2025/UniMotion_Unifying_3D_Human_Motion_Synthesis_and_Understanding]]

启发：
1. 多级语义注入：帧级、序列级
2. 帧级文本-动作时序对齐
这篇工作直接将我对MoLingo的SAE的两个改进路线囊括了，虽然这篇是在动作空间执行而不是表征空间，但依然pass了我对molingo SAE的修改。

核心：
1. HML+BABEL训练，模型具备帧级和序列级语义理解能力
2. diffusion同时对动作和文本去噪，两种模态使用独立的扩散时间步（从而可以m2t和t2m，已知一种模态，生成另一种模态；或从），支持sequence text condition下的motion2frame_text、frame_text2motion、condition_driven <motion,frame_text>生成 三种范式；

![[assets/figures/papers/paper_list_l2_https_coral79_github_io_uni_motion_paper_unimotion_pdf/figures/012_Figure.jpg]]
*Figure: (b)*

![[assets/figures/papers/paper_list_l2_https_coral79_github_io_uni_motion_paper_unimotion_pdf/figures/016_Figure_11.jpg]]
*Figure 11: Motion understanding comparison with MotionGPT [17]. MotionGPT is capable of performing multiple tasks, including motion captioning and question answering. We tasked both MotionGPT (left) and Unimotion (right) with understanding an input motion by breaking it down into motion segments. However, due to MotionGPT’s lack of temporal awareness, it was unable to successfully complete this task. Specifically, instead of answering with multiple motion segments, it just predicts an incorrect length for the whole sequence (A: “The motion lasts for approximately 2.5 seconds.”). In contrast, our model is the first to understand motion both semantically and temporally*


![[assets/figures/papers/paper_list_l2_https_coral79_github_io_uni_motion_paper_unimotion_pdf/figures/002_Figure_2.jpg]]*Figure 2: Overview of UniMotion. UniMotion is a transformer-based diffusion model (Model) that can be input conditioned on a) human motion, b) clip embedded frame-level text, or c) sequence-level text (Input) or any subsets thereof or none, and instead supplied with noise. At it’s core it allows to diffuse motion and text individually, implemented via separate denoising timesteps t ^ { x } and t ^ { y } . . After training with Frame-level text Losses and Motion losses (Loss), see Sec. 4.1. UniMotion can output clean, noise-free motion, and frame-level text descriptions explaining the generated motions. (Output)*



## [[analysis/CVPR_2025/AC3D_Analyzing_and_Improving_3D_Camera_Control_in_Video_Diffusion_Transformers]]

启发点：相机条件在不同去噪步注入约束，在预训练VDiT主干的浅层注入。这说明模型可能已经隐式学会了所需的知识，但是不会显示地调用。

待明确：
1. 选择pretrained model的标准是什么
2. 用于判断模型隐式学习到相机知识的标准是什么，为什么这条计算可以明确证明？
3. 动作生成，如何迁移本文的问题和解法？是否是从pretrained motion genrator挖掘、封装能力（类似）

**核心洞察。** 线性探测实验进一步揭示，冻结的大规模视频扩散Transformer（VDiT）内部隐式编码了精确的相机姿态知识——旋转误差仅0.025，平移误差0.48，且该信息集中于中间层（#9–#21层）达到峰值（Figure 5）。这意味着模型在浅层即可完成相机姿态的解析与传递，深层应专注于视觉渲染。

**方法定位。** 基于上述分析，AC3D通过三个关键设计限定相机控制的作用域，从而突破精度与质量之间的权衡：

1. **时间域截断**：将训练噪声采样和推理时的相机条件注入限制在反向扩散的前40%时间步（[0.6, 1]区间），仅在低频生成阶段施加控制。
2. **空间域截断**：仅将相机条件注入前8个DiT块（浅层），后24个块不受相机条件影响，使深层专注于高频视觉特征合成。
3. **数据去偏**：额外采集20K动态场景但固定相机的视频，解耦相机运动与场景静止的虚假关联。


## [[FLD_Fourier_Latent_Dynamics_for_Structured_Motion_Representation_and_Learning]]

启发：周期性动作可以用潜在动力学进行多步预测，用极少参数量表示动作序列。text2motion更多是非周期性动作。本文是否有机会进行机制设计，来从周期性动作扩充到text2motion的能力？


## [[analysis/CVPR_2025/MotiF_Making_Text_Count_in_Image_Animation_with_Motion_Focal_Loss]]

启发：
1. 自适应的加权生成有机会用于提高核心动作、关节点的关注，从而提高文本-运动对齐，以及生成动作质量。
2. 训练获得能力，推理无感启用能力：
	1. MotiF利用光流（optical flow）从训练视频中提取运动强度，生成运动热力图（motion heatmap），并将其作为逐像素权重乘到扩散损失上。这使得高运动区域的预测误差被放大，迫使模型在训练中将优化重心从静态背景转移到运动物体上。该方法在推理时**无需任何额外输入**，因为运动焦点信号仅作用于训练阶段，模型在推理时仍仅接收图像和文本，但已通过聚焦训练学会了更好地遵循文本运动指令。
	2. text2motion，能否也利用gt数据先聚集关键动作、关键点、关键片段等，让模型在训练阶段获得能力，在推理阶段自主运用能力？
		1. 关键在于，从图像生成视频，是有大量精致区域的；而动作序列本身就全身运动。为了迁移范式，我想到以下两种未经验证的初步思路：
			1. 动转静：动作数据的时域转频域，如利用[[FLD_Fourier_Latent_Dynamics_for_Structured_Motion_Representation_and_Learning]]、[[PAE]](SIGGRAPH 22)，或者的模型，或者仅仅是时域-频域转换（可以joint-level）
			2. 直接在动上设计关注点
			3. 让新的agent不参考我上述两个想法，自行设计，与ds讨论

难点：如何让模型学会关注点，如何提供关注点的训练引导数据？

# 暂时没想到具体启发

## [[analysis/3DV_2026/Appreciate_the_View_A_Task-Aware_Evaluation_Framework_for_Novel_View_Synthesis]]

现有评估指标无法有效捕捉<源图像、目标视角、生成结果>的三元关系，具体是
+ 其一，全参考指标常对合理的视角变化施加错误惩罚
+ 其二，无参考分布指标对视角一致性完全不敏感，无法区分生成质量的高低
本文利用**扩散式 NVS 模型（Zero123）的中间特征天然编码了视角相关的几何与外观信息**。通过在三元组（源图像、目标图像、相机姿态）上提取多尺度扩散特征，并经轻量对比学习微调为紧凑的 L2 归一化嵌入，PRISM 同时支持全参考和无参考分布两种互补的评估模式。

PRISM 框架的核心设计理念是：将新视角合成（NVS）的评估建模为对**三元组（源视图、目标视图、相机姿态）**的语义理解问题，而非简单的像素级或分布级比较。如图 Figure 2 所示，整个框架由两个阶段构成：**特征提取流水线**和**双模式评估**。

![[assets/figures/papers/paper_list_l5_https_arxiv_org_abs_2511_12675/figures/002_Figure_2.jpg]]
*Figure 2: Method Overview. (Left) Feature extraction: given source, target, and camera transformation, we noise the target image and extract features from a diffusion-based NVS model. These are pooled and tuned into fPRISM. (Right) Evaluation framework: Full-Reference: measure distance between fPRISM of a predicted triplet and its ground-truth counterpart. No-Reference: compute MMD between fPRISM from generated triplets and an anchor set of real triplets*


## [[analysis/arXiv_2024/MotionCLR_Pay_Attention_and_Move_Better_Harnessing_Attention_for_Interactive_Motion_Generation_and_Training-free_Editing]]

关注：这篇分析了SA和CA在motion generation中的分工，并小合集了motion edit任务进行验证.(de)-emphasizing, in-place motion replacement, and example-based motion generation \_etc.
或许对动作生成的特征传播作用分析有借鉴价值。



## [[analysis/NeurIPS_2024/ReVideo_Remake_a_Video_with_Motion_and_Content_Control]]

目前没有直接的启发，但留着当候选。提及的`内容和动作解耦`的扩散自适应加权，以及三阶段训练可以参考。

### 三阶段训练策略

训练策略是 ReVideo 实现内容-运动解耦的另一关键。三阶段训练从粗到细逐步建立控制能力：

1. **运动先验训练**：仅使用运动条件训练控制模块，使模型初步建立对轨迹信号的响应能力。
2. **解耦训练**：核心创新在于数据构造策略——将训练样本 $V$ 的编辑区域与未编辑区域分别取自两个不同的视频：$V = V_1 \cdot M + V_2 \cdot (1 - M)$。这迫使模型无法从未编辑区域推断编辑区域的运动，必须依赖显式的运动轨迹条件。此阶段同时引入内容控制，实现两种条件的初步解耦。
3. **去块训练**：解耦训练后编辑边界可能出现块效应。此阶段仅微调控制模块和基础模型 SVD 中时序自注意力层的 Key/Value 嵌入（$W_k$ 和 $W_v$），在消除块伪影的同时保持已建立的局部运动控制能力。消融实验表明，若在此阶段微调整个控制模块，局部运动控制精度会下降（Figure 7）。

## # NoiseCLR

关注：pretrained T2I模型的编辑能力挖掘和封装，从而能够适应任何new image的edit





---

# 调整
## 需要直接删除的条目



## 信息调整条目
arxiv的论文需要gpt5.4复核是否已经中稿。提供了project/code的工作很大概率已经中稿，可以顺着提供的链接查看。例如

## 图片裁切问题
1. [[analysis/arXiv_2024/MotiF_Making_Text_Count_in_Image_Animation_with_Motion_Focal_Loss]]的figure1

## 补充分析的论文


---

# 想法

## 