---
created: 2024-06-17T14:21
updated: 2026-04-08T13:19
---

# 禁止上传开源的 BITE 仓库
# 组内服务器的飞书使用说明
[Feishu - Log in](https://exz1kb25u1.feishu.cn/docx/IRBld7PqRov6dhx9N5vcaNcNnzd)
```bash
~/auth_client -u 52285901053 -p Manbo.02641263 auth 
```

```
~/ecnu_server_network/auth_client -u 52285901053 -p Manbo.02641263 auth 
```

~/ecnu_server_network/auth_client -u <工号> -p \<passwd> auth 

## 常用代码：

查看进程user：`ps -o user= -p <pid>`
```bash
ps -o user= -p 
```

### 5090：
```
ripemangobox@172.23.148.136:
```

```
NoWay2030System
```

系 统: Ubuntu 22.04
用户名:user
密 码:admin@123
用户名:root
密码:admin@123
BMC IP 地址:192.168.100.1
用户名/密码:admin/Server@123

### 4090：
```
ripemangobox@172.23.148.106:
```

```
sudo mkdir -p /mnt/sda
sudo mount /dev/sda /mnt/sda
```

```
NoProblem2035Power
```

### H20：
```
public@172.23.148.117:
```
```
Xunifangzhen0
```

查看user `public`的进程
```bash
ps -u public --forest
```

### L40：
```
ripemangobox@172.23.148.80:
```

```
ripemangobox@172.23.148.80:/home/ripemangobox/l_ripemangobox/Motion
```

su
```
ecnuCVCG24RandoM
```

### yy L40
```
yyqi@172.23.148.80:
```

```
QYY#20103
```

工位机锁屏
```
20103
```

调试必须打开绝对的文件夹，不能使用软连接为根目录。所以vsode打开以下文件夹即可
```
/sata/public/ripemangobox/Motion/InterGen
```

# 免密登录

```bash
ssh-keygen
```

### windows
[windows配置ssh免密登录 - Cloud↓Sky - 博客园](https://www.cnblogs.com/liuyangjun/p/16615552.html)

### Linux
```
ssh-keygen
```

##### 密码重置
[Ubuntu 出现Host key verification failed解决方案\_ubuntu host key verification failed.-CSDN博客](https://blog.csdn.net/qq_29680161/article/details/104687350)

## 镜像源
**一次性使用方法**：
```bash
 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

```bash
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
```
### 常用的国内镜像源
1. **清华大学镜像源**：
```
https://pypi.tuna.tsinghua.edu.cn/simple
```
2. **豆瓣镜像源**：
```
https://pypi.douban.com/simple
```
3. **阿里云镜像源**：
```
https://mirrors.aliyun.com/pypi/simple
```
4. **中国科技大学镜像源**：
```
https://pypi.mirrors.ustc.edu.cn/simple
```
5. **腾讯云镜像源**：
```
https://mirrors.cloud.tencent.com/pypi/simple
```

## 关闭构建隔离
```
pip install --no-build-isolation -i https://pypi.tuna.tsinghua.edu.cn/simple
```
注：`--no-build-isolation` 会告诉 pip 直接使用你当前 DART 环境里的 setuptools 和依赖，而不再去创建那个会找不到 pip 的临时环境。

# Tmux教程（用于解绑窗口和进程，防止进程因断网被切断）
[Tmux 使用教程 - 阮一峰的网络日志](https://www.ruanyifeng.com/blog/2019/10/tmux.html)

# 软连接教程
[linux 软连接的使用 - 少说点话 - 博客园](https://www.cnblogs.com/sueyyyy/p/10985443.html)



# 本地与服务器的文件传输
Link：[【Ubuntu】将本地文件复制到服务器上\_ubuntu复制本地文件到服务器的命令-CSDN博客](https://blog.csdn.net/u013066730/article/details/121400127)
## 本地到服务器
文件
```bash
scp  /home/work/111.jpg ripemangobox@172.23.148.80:/sata/public/ripemangobox/Motion/Duolando-Server 
```
scp  /home/ripemangobox/.ssh/id_rsa.pub ripemangobox@172.23.139.80:/home/ripemangobox:/sata/public/ripemangobox/Motion/Duolando-Server 
文件夹
```bash
scp  -r  /home/work/  ripemangobox@172.23.148.80:/sata/public/ripemangobox/Motion/Duolando-Server 
```

## 从远程到本地
文件
```bash
scp   ripemangobox@172.23.148.80:/sata/public/ripemangobox/Motion/Duolando-Server /home/work/111.jpg
```

文件夹
```bash
scp  -r    ripemangobox@172.23.148.80:/sata/public/ripemangobox/Motion/Duolando-Server /home/work/
```


## H20 推理报错
原因：可能是半精度推理会问题
解决
1. 卸载conda版torch，换为pip版；
2. 更新cuda：
```bash
   pip install nvidia-cublas-cu12==12.4.5.8
   ```
3. 如果还不行，强制使用半精推理（不清楚原因）`with autocast(enabled=True):`

## HF 无法下载文件
出现问题
```

```

解决方法：使用镜像网站
```bash
https://hf-mirror.com
```

## Mamba
```bash
# 安装 mamba
conda install mamba -n base -c conda-forge

# 添加 pytorch 镜像
conda config --set custom_channels.pytorch https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/

# 添加 nvidia 镜像
conda config --set custom_channels.nvidia https://mirrors.cernet.edu.cn/anaconda-extra/cloud/

# 安装 pytorch
mamba install pytorch==2.1.0 pytorch-cuda=11.8 -c pytorch -c nvidia
```

## HF Token
```
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
```


## 文件下载
✅ 推荐方法 1：使用 `aria2c`（多线程下载，速度快）
```bash
aria2c -x 16 -s 16 <url>
```
- `-x 16`: 允许的最大连接数为 16
- `-s 16`: 使用 16 个分段同时下载

✅ 方法 2：使用 `wget`（支持断点续传）
```bash
wget -c <url>
```
- `-c` 表示断点续传，如果下载中断可以继续下载而不需要重新开始。

## ubuntu文件查找
```
fdfind <file_name>
```
fdfind效率很高

## FFmpeg
报错如下
```
subprocess.CalledProcessError: Command '['ffmpeg', '-r', '30', '-i', '_tmp%07d.png', '-vframes', '66', '-vcodec', 'h264', '-pix_fmt', 'yuv420p', '-y', './results/generated/gt/intergen/Lumba_005_001.mp4']' returned non-zero exit status 1.
```

解决：运行
```bash
conda install -c conda-forge x264 ffmpeg -c conda-forge --override-channels
```

## moviePy pip 安装失效
使用conda安装，一次就成。网络不佳就重试
```bash
conda install -c conda-forge moviePy==1.0.3 --override-channels
```

## Pytorch3d
该网址有一系列pytorch3d wheel
```bash
[Prebuilt wheels provided via 3rd party repository · facebookresearch/pytorch3d · Discussion #1752 · GitHub](https://github.com/facebookresearch/pytorch3d/discussions/1752)
```

使用方式：
```bash
pip install --extra-index-url https://miropsota.github.io/torch_packages_builder pytorch3d==0.7.7+pt2.1.0cu121
```

torch version查看
```
python
import torch
torch.__version__
```

## git仓库新建与链接
1、如果还没有初始化
```bash
git init
git add .
git commit -m "Initial commit"
```

2、在github页面创建仓库，并复制ssh链接如下：
```bash
git remote add origin <ssh url>
```

3.1、[optional] 强制合并远程内容（如果你希望保留远程和本地内容）
```bash
git pull origin main --allow-unrelated-histories
```

3.2、如果直接以本地为准，覆盖远端
```bash
git fetch origin main
git reset
```

4、解决冲突后提交
```bash
git add .
git commit -m "Merge local and remote histories"
```

5、推送合并后的内容
```bash
git push -u origin master
```
😜🎉🎊


## 按用户查看占用
```
ncdu /
```
如果查看别的盘，按照`ncdu <disk>`修改即可，如`ncdu /data`
## 文件大小筛选显示
显示所有大于100M的文件
```bash
du -h --max-depth=1 . | awk '$1 ~ /[0-9]+(\.[0-9]+)?[GM]/ {if ($1 ~ /G/ || ($1 ~ /M/ && $1+0 >= 100)) print $0}'
```
### **命令说明**
1. **du -h --max-depth=1**：
    - du：磁盘使用量统计工具。
    - -h：以人类可读格式输出大小（例如，MB、GB）。
    - --max-depth=1：仅显示指定目录下第一级子文件夹的大小（不递归到更深层）。
    - /path/to/directory：替换为要扫描的目录路径（例如，/home/user）。
2. **awk**：
    - $1 ~ /[0-9]+(\.[0-9]+)?[GM]/：匹配大小字段（第一列），确保是数字后跟 M（MB）或 G（GB）的格式。
    - if ($1 ~ /G/ || ($1 ~ /M/ && $1+0 >= 100))：
        - 如果大小以 G（GB）结尾，直接输出（因为 1GB > 100MB）。
        - 如果大小以 M（MB）结尾，且数值大于或等于 100，输出。
    - print $0：打印符合条件的整行（大小和文件夹路径）。

查看不同用户的占用量，其中`/sata/public`为统计的路径
```bash
sudo du -sh /sata/public/* 2>/dev/null | sort -hr
```


PIN
```
2025
```


账号：
```
ECNUGqh@outlook.com
```

账号密码：
```
ecnuCVCG24RandoM
```


## git ssh 自动替换 https
```
git config --global url."git@github.com:".insteadOf "https://github.com/"
```

## 先git clone，然后发现需要建立fork仓库链接的情况
操作流程

### 奇淫技巧
1.fork仓库
2.去服务器git clone fork仓库（之所以在服务器，是因为服务器git clone网速快）
3.将克隆仓库的.git复制到本地仓库，完成同步

### 正经操作流程
1.github上fork/创建仓库
2.删除本地仓库
```bash
rm -rf .git
```
3.初始化仓库
```bash
git init
```
4.创建本地仓库
```bash
git checkout -b main
```
5.关联远程仓库
```bash
git remote add origin <ssh/https> 
```
6.先拉取仓库信息
```bash
git pull
```
如果遇到git pull和git push出现矛盾，就
```bash
git rebase --continue
```
以本地进度为主。

7.add、commit、push，在push时会提示关联具体的上游仓库，然后完成。

8.[Optional]撤回最近一次commit，并保留修改
```
git reset --soft HEAD~1
```

## 文件夹Input/Output Error
出现例如以下问题
![[Pasted image 20250917151421.png]]

解决措施：
1.unmount
```bash
sudo umount /dev/sda1
```
2.修复
```bash
sudo fsck /dev/sda1
```
3.重新挂载


## hf mirror
```bash
echo 'export HF_ENDPOINT=https://hf-mirror.com' >> ~/.zshrc
source ~/.zshrc
```

## 服务器IP解锁
1.查看被ban的IP
```bash
sudo fail2ban-client status sshd
```

2.解锁IP
```bash
sudo fail2ban-client set sshd unbanip <被锁定的IP地址>
```



## 新用户添加与文件夹权限处理
1.查看现有用户
```bash
cat /etc/passwd
```

2.查看当前登录用户
```bash
whoami        # 显示当前用户名
w             # 显示当前所有登录用户及活动
users         # 简洁列出登录用户名
```

3.交互式创建新用户
```bash
sudo adduser <newusername>
```

模拟登录
```
su - <username>
```

## 用户磁盘用量统计
### 看数据盘（注意/data替换为实际数据盘）
```
sudo ncdu /data
```

### 看系统盘
```
sudo du -sh /home/* | sort -h
```

## 文件夹权限设置
#### 1. 修复父目录的所有权和权限

这部分实现你的前两个目标：让 `/data` 和 `/data/public` 归 `root:root` 所有，并设置权限为 `755` (rwxr-xr-x)，以便其他用户可以 "查看" 和 "穿越"。

```bash
sudo chown root:root /data
sudo chown root:root /data/public

sudo chmod 755 /data
sudo chmod 755 /data/public
```


修复所有用户目录的所有权和权限
```bash
# 遍历当前目录下的所有子目录 (e.g., clgx/, hyj/, ripemangobox/)
for username in */; do
    
    # 清理目录名，去掉末尾的 '/'
    username_clean=${username%/}
    
    # 确保它是一个有效的目录名
    if [ -n "$username_clean" ] && [ "$username_clean" != "." ] && [ "$username_clean" != ".." ]; then
        
        echo "正在设置目录: $username_clean"
        
        # 1. 将目录所有者和组都设置为其用户名
        #    例如：clgx 目录 -> chown -R clgx:clgx clgx
        #    这会移除你所说的 "public" 组
        sudo chown -R "$username_clean:$username_clean" "$username_clean"
        
        # 2. 将目录权限设置为 701 (rwx-----x)
        #    这样脚本或其他程序(作为 Other)可以 "cd" 进去，或者读取已知路径的文件 # 但它们无法执行 "ls" 看到里面有什么
        sudo chmod 701 "$username_clean"
        
    fi
done

echo "所有用户目录权限已重置完毕。"
```
（**注意**：此脚本假设用户名和目录名完全一致，例如用户 `clgx` 对应目录 `clgx`。如果你的系统上没有 `clgx` 这个用户，`chown` 会报错。）


# 从 auth.log 中提取所有失败尝试的 IP，并统计次数
## ECNU-1X Ubuntu Wifi
![[Pasted image 20260124144447.png]]



我们的5090服务器在寒假期间被入侵，现已进行相关处理。

同学们需要进行以下操作：
1. 登录：使用49281端口连接，如
   `ssh -p 49281 <user>@172.23.148.136`
2. 有账号的同学尽快修改密码，登录5090后使用`passwd`修改密码.


## Tmux
创建 `tmux.conf` 并写入鼠标滑动
```
echo "set -g mouse on" >> ~/.tmux.conf
```

刷新配置
```
tmux source ~/.tmux.conf
```

## CC
