# base_v0_20260515

## Role

Old/base version for analysis-chain comparison and rollback.

## Git Reference

- Baseline commit: `3ce08a7 feat: finalize local paper analysis chain`
- Branch context: `experiment/analysis-chain-batch-eval`

## Behavior

- Produces formal local paper-analysis notes from MinerU parsed PDFs.
- Uses the earlier writer/report flow before the cache-oriented defaults and matrix test runner.
- Existing Obsidian notes in `obsidian-vault/analysis/` are treated as old-version comparison outputs when the same paper exists.

## Known Limitations

- Less controlled batch/variant isolation.
- No generic named variant matrix runner.
- `--vault-root` did not imply a matching asset root in isolated vault tests; assets could remain under the default vault unless explicitly overridden.
- Baseline citation formatting in lineage sections was not explicitly requested in the prompt contract.

## Use

Use only as the base comparator. Do not promote this version as the default if `analysis_chain_v1_20260522` passes manual quality review.
