\section{Experiments}

\subsection{Protocol}

Report the controlled matched available-data protocol, original Pulp captions, C0-LAT
operational mainline, C0-GEO audited objective alternate, non-causal sampler, and complete
version/run identity. Native and adapted baselines must retain their own representation,
decoder, mode, and assurance boundary.

\subsection{Main system comparison}

\begin{table}[t]
  \centering
  \caption{Complete-system comparison recipe. Formal values must be populated from one audited
  row per system; every row requires a non-empty version/run identity.}
  \label{tab:main}
  \small
  \begin{tabular}{lllll}
    \toprule
    Version / run & Role & Human & Observed-H Camera & Generated-H pair \\
    \midrule
    \multicolumn{5}{c}{\textit{Pending audited row selection from the canonical metric ledger}} \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Claim-changing controls}

Separate Phase-C gradient/update provenance, symmetric routes, Interaction16, no-source cleanup,
and Camera dropout. Report matched fields and covariates explicitly; mixed Pareto results must
remain field-wise.

\subsection{Camera-text response and CFG}

\evidencepending. Freeze Human motion, sample identity, length, initial noise, sampler, and
decoder while comparing correct, fixed-point-free shuffled, contradictory, and absent Camera
text. Select one CFG scale on validation data before opening pure test.

\subsection{Simple Human-to-Camera composition}

\evidencepending. First audit representation and coordinate compatibility. Label every pair as
native or adapted, record whether Camera text is consumed, and distinguish zero-retraining
composition from a separately authorized adapter.

\subsection{Qualitative and resource evidence}

No visual-superiority or failure-rate statement is allowed before a sealed random/best/worst
blind audit. Report Human-pass, Camera-pass, and total two-pass latency, parameters, peak memory,
throughput, and length scaling under one frozen runtime contract.
